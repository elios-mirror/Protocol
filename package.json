{
  "name": "elios-protocol",
  "version": "1.0.0-snapshot-1.0",
  "description": "Node.js & C++ Elios Protocol",
  "main": "index.js",
  "gypfile": true,
  "dependencies": {
    "bindings": "~1.5.0",
    "node-addon-api": "^1.6.3"
  },
  "scripts": {
    "test": "jest --coverage",
    "prepublish": "npm build ."
  },
  "devDependencies": {
    "jest": "^24.8.0"
  }
}
